{
  "_comment": "PostToolUse hook: Auto-format TypeScript/TSX files after edits using Biome",
  "type": "PostToolUse",
  "matcher": "Edit",
  "config": {
    "hooks": [
      {
        "type": "command",
        "command": "npx biome format --write \"$TOOL_INPUT_FILE_PATH\" 2>/dev/null || true"
      }
    ]
  },
  "installation": "Add to .claude/settings.json under hooks.PostToolUse[]",
  "notes": "Falls back gracefully if Biome is not installed. Replace with prettier if using that formatter."
}
