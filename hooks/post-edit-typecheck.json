{
  "_comment": "PostToolUse hook: Run TypeScript type checking after editing .ts/.tsx files",
  "type": "PostToolUse",
  "matcher": "Edit",
  "config": {
    "hooks": [
      {
        "type": "command",
        "command": "npx tsc --noEmit --pretty 2>&1 | head -20 || true"
      }
    ]
  },
  "installation": "Add to .claude/settings.json under hooks.PostToolUse[]",
  "notes": "Limits output to 20 lines to avoid flooding context. Runs after every edit."
}
